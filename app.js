const nav=document.getElementById("nav");window.addEventListener("scroll",()=>nav&&nav.classList.toggle("scrolled",scrollY>40));
const menu=document.querySelector(".menu");if(menu)menu.addEventListener("click",()=>{const n=document.querySelector(".nav nav");if(!n)return;n.style.display=n.style.display==="flex"?"none":"flex";n.style.position="absolute";n.style.top="82px";n.style.left="0";n.style.right="0";n.style.padding="25px";n.style.background="rgba(3,4,5,.96)";n.style.flexDirection="column"});
const canvas=document.getElementById("fluidCanvas");
if(canvas){const c=canvas.getContext("2d");let w,h,pts=[];let mx=innerWidth/2,my=innerHeight/2;function resize(){w=canvas.width=innerWidth;h=canvas.height=innerHeight}resize();addEventListener("resize",resize);addEventListener("mousemove",e=>{mx=e.clientX;my=e.clientY});for(let i=0;i<520;i++)pts.push({x:Math.random()*innerWidth,y:Math.random()*innerHeight,bx:Math.random()*innerWidth,by:Math.random()*innerHeight,s:Math.random()*.8+.15});function loop(t){c.clearRect(0,0,w,h);for(const p of pts){const tx=p.bx+Math.sin(t*.00035+p.by*.008)*45,ty=p.by+Math.cos(t*.0003+p.bx*.006)*30,dx=mx-p.x,dy=my-p.y,d=Math.hypot(dx,dy);if(d<190){p.x-=dx*(190-d)/190*.025;p.y-=dy*(190-d)/190*.025}p.x+=(tx-p.x)*.012;p.y+=(ty-p.y)*.012;c.fillStyle="rgba(255,255,255,.38)";c.beginPath();c.arc(p.x,p.y,p.s,0,Math.PI*2);c.fill()}requestAnimationFrame(loop)}requestAnimationFrame(loop)}
const wave=document.getElementById("waveCanvas");if(wave){const x=wave.getContext("2d");function size(){wave.width=innerWidth;wave.height=innerHeight}size();addEventListener("resize",size);function draw(t){x.clearRect(0,0,wave.width,wave.height);for(let r=0;r<14;r++){x.beginPath();for(let a=0;a<Math.PI*2;a+=.04){let rad=80+r*23+Math.sin(a*5+t*.001+r)*18;let px=wave.width/2+Math.cos(a)*rad,py=wave.height/2+Math.sin(a)*rad;x.lineTo(px,py)}x.closePath();x.strokeStyle=`rgba(255,255,255,${.02+r*.003})`;x.stroke()}requestAnimationFrame(draw)}requestAnimationFrame(draw)}
document.querySelectorAll(".button,.nav-cta").forEach(b=>b.addEventListener("mousemove",e=>{const r=b.getBoundingClientRect();b.style.transform=`translate(${(e.clientX-r.left-r.width/2)*.08}px,${(e.clientY-r.top-r.height/2)*.08}px)`;}));